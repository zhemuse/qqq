{
  "name": "qqq-agent-v2",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/cli/index.ts",
    "test": "bun test",
    "check:types": "tsc --noEmit",
    "check": "bun test && bun run check:types && bun build src/cli/index.ts --target bun --outfile /tmp/qqq-agent-v2-check.js"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^5.9.3"
  }
}
